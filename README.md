Automation test suite scaffold created. Update support/world.js BASE_URL and test credentials before running.

To install and run:

cd automation-suite
npm install
npx playwright install
npm run test

